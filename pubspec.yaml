name: login_signup
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.40.0
  flutter_svg: 2.0.17
  cupertino_icons: ^1.0.2
  firebase_auth: ^5.4.0
  cloud_firestore: ^5.6.1
  bot_toast: ^4.1.3
  go_router: ^14.6.3
  image_picker: ^1.1.2
  provider: ^6.1.2
  firebase_core: ^3.10.0
  flutter_launcher_icons: ^0.14.2
  logger: ^2.5.0
  flutter_map:  
  latlong2: 
  location: 
  http:
  collection:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/company_logo.png"
flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/Ryadhmetro.json

dependency_overrides:
  fl_chart:
    path: lib/packages/fl_chart